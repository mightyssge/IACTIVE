# Samples

- `draft_llm_example.json`: ejemplo de salida cruda del microservicio LLM (sin totales).
- `invoice_final_example.json`: ejemplo de factura final calculada.
- PDF/HTML se generan en tiempo de ejecución; revisa el endpoint `/api/invoices/{id}/pdf` y la respuesta `html_preview` para verlos.
